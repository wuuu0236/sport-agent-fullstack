# sport-agent 全栈化架构方案（混合栈）

> 目标：把现有 Python 零依赖 MVP（`sport-agent-mvp`）升级为产品级**全栈多智能体应用**。
> 核心思路：**保留现有 Python Agent 逻辑零改动**，新增 Spring Boot 编排网关 + Vue 协作看板，
> 让多 Agent 从「并联分发」升级为「编排协作、肉眼可见」。

---

## 1. 技术选型

| 层 | 技术 | 说明 |
|---|---|---|
| 前端 | Vue 3 + Vite | 对话界面 + **Agent 链路看板**（SSE 实时展示谁在干活、产出什么） |
| API 网关 | Spring Boot 3 | REST + SSE 统一入口，负责鉴权/限流/路由 |
| 编排层 | LangGraph4j | 状态图定义 Agent 流转；**节点 = 调一次 Python Agent**，边 = 状态传递 |
| Agent 能力层 | Python (FastAPI) 微服务 | 把现有 `coach / posture / research / writer / memory / scheduler` 包成 HTTP 端点 |
| 记忆召回 | 向量库（chroma / 本地 bge-small-zh） | 替代关键词重叠，补上 RAG 语义召回短板 |
| 结构化数据 | MySQL | 替代 `sessions/*.json` 文件，持久化训练记录 |
| 双存储记忆 | `USER.md` + `MEMORY.md` | 冻结快照注入 system prompt（沿用 MVP 设计） |
| 部署 | docker-compose | 三服务一键编排 |

**为什么是混合栈而不是纯 Java / 纯 Python：**
- 纯 Java：要把现有 Python Agent 全部重写，工作量大且丢失已有验证过的逻辑；
- 纯 Python（FastAPI+LangGraph）：Agent 零改动，但你已点名 Spring Boot、且「Java 编排调度 Python 微服务」是更强的面试叙事；
- 混合栈：Spring Boot 做「大脑」（编排+网关），Python Agent 做「手脚」（AI 能力），各司其职。

---

## 2. 目录结构

```
sport-agent-fullstack/
├── backend/                      # Spring Boot (Java) —— 编排网关
│   ├── src/main/java/.../
│   │   ├── controller/           # REST + SSE 端点（/api/chat, /api/stream, /api/state）
│   │   ├── orchestrator/         # LangGraph4j 状态图定义（节点=调 Python Agent）
│   │   ├── client/               # 调 Python Agent 的 HTTP 客户端（带重试/超时）
│   │   └── model/                # DTO / 状态对象
│   └── pom.xml
│
├── agent-service/                # Python Agent 微服务（保留现有逻辑）
│   ├── src/
│   │   ├── agents/               # 从 MVP 搬过来的 coach/posture/research/writer/memory/scheduler
│   │   ├── supervisor.py         # 保留（单 Agent 内部仍可路由）
│   │   ├── memory.py / skill_loader.py
│   │   └── llm.py
│   ├── app.py                    # FastAPI: POST /agent/{name}
│   └── requirements.txt          # fastapi, uvicorn, openai, rapidocr-onnxruntime
│
├── frontend/                     # Vue 3
│   ├── src/
│   │   ├── views/Chat.vue        # 对话主界面
│   │   ├── components/AgentChainBoard.vue  # 多 Agent 协作链路可视化
│   │   └── api/client.ts         # SSE 订阅
│   └── vite.config.ts
│
├── docker-compose.yml            # backend + agent-service + frontend 三服务
└── README.md
```

---

## 3. 核心流程：以「写本周训练周报」为例

这是多 Agent **真正协作**的示范场景（直接回应此前「多 Agent 没发挥作用」的质疑）：

```
START
  └─> research   (调 Python research Agent：搜最新训练理论)      ── state.research = 摘要
  └─> coach      (调 coach Agent：拉本周数据 + 做负荷/区间分析)  ── state.analysis = 报告
  └─> writer     (调 writer Agent：汇成周报)                    ── state.report  = 成稿
  └─> END  →  把 state.report 通过 SSE 推回前端
```

- 每一步的**产出写入共享 state**，下一步**读取上游 state** —— 这是和「单 LLM + 多 prompt」的本质区别；
- 编排进度通过 **SSE 实时推给前端 `AgentChainBoard`**，用户能看到 `research 搜完 → coach 分析中 → writer 成稿` 的链路。

---

## 4. 接口契约

### 4.1 前端 ↔ Spring Boot
```
POST /api/chat        { "message": "给我写本周训练周报" }
  → 200 { "taskId": "t_xxx" }

GET  /api/stream/{taskId}     (SSE, text/event-stream)
  → event: agent_start   { "agent": "research", "status": "running" }
  → event: agent_chunk   { "agent": "research", "partial": "..." }
  → event: agent_done    { "agent": "research", "output": "..." }
  → event: task_done     { "final": "本周训练周报全文..." }

GET  /api/state         → 记忆 / 训练数据快照（给前端展示「助理记得你什么」）
```

### 4.2 Spring Boot ↔ Python Agent
```
POST http://agent-service:8001/agent/{name}
  body: { "input": "用户原始诉求或上游 state",
          "context": { "user_md": "...", "memory_md": "...", "upstream_state": {...} } }
  → 200 { "output": "Agent 产出文本",
          "metadata": { "used_skill": "coach_hr_zones", "tokens": 1234 } }
```
统一契约：input 带上游 state，output 返回结构化结果，便于编排层拼接。

---

## 5. 数据模型

**MySQL（结构化）：**
- `users`：用户基础信息、静息/最大心率
- `sessions`：训练记录（距离、时长、配速、心率区间、来源 GPX/OCR）
- `agent_runs`：编排日志（taskId、走过的节点、耗时、token）

**向量库（语义记忆）：**
- `memory_chunks`：把 `USER.md` / `MEMORY.md` 切块 → bge-small-zh embedding → 余弦召回 top-k

**双存储 MD（冻结快照，沿用 MVP）：**
- `data/USER.md`：关于用户的事实
- `data/MEMORY.md`：助理笔记
- 启动时渲染成快照注入 system prompt（保前缀缓存）

---

## 6. 演进路线（分阶段，每阶段可独立验证）

| 阶段 | 内容 | 验收标准 |
|---|---|---|
| 0 骨架 | 三服务起来、ping 通、docker-compose 一键起 | 前端能发消息到 Spring Boot，Spring Boot 能调通 Python Agent 回显 |
| 1 编排 | LangGraph4j 跑通「写周报」流水线 | 前端看板看到 research→coach→writer 链路与各自产出 |
| 2 记忆 | 向量库替代关键词重叠 | 问「我最大心率多少」能语义召回，不再靠关键词 |
| 3 持久化 | MySQL 替代 JSON 文件 | 训练记录入库，刷新不丢 |
| 4 产品化 | 看板美化 + 微信/企业微信接入 | 多端可用，链路展示美观 |

> 建议先只做 **阶段 0 + 阶段 1**，这是性价比最高、最对症「多 Agent 没发挥作用」质疑的一步。

---

## 7. 面试叙事要点（照你的「生活化 + 筋骨技术词」风格）

- **一句话定位**：「用 Spring Boot 做编排网关，统一调度 Python 写的多智能体微服务，
  把教练/研究/写作 Agent 串成一条协作流水线，而不是一个 prompt 包打天下。」
- **硬差异（能经得起追问）**：
  1. 多 Agent **编排协作**：LangGraph4j 状态图 + 共享 state 传递，每一步产物喂下一步；
  2. **跨语言架构**：Java 编排层 + Python AI 能力层，进程隔离、可独立扩缩；
  3. **双存储记忆 + 向量召回**：跨会话记得用户，语义检索而非关键词；
  4. **Skill 三级渐进加载 + 自蒸馏**：越用越聪明。
- **诚实短板（被问要敢说）**：单 LLM（DeepSeek）、Skill 蒸馏是手动触发（非 Hermes 全自动）、
  MVP 阶段未接 MCP、微信入口仅预留。

---

## 8. 与现有 MVP 的关系

- `agent-service/` 直接复用 `sport-agent-mvp/src/` 的 Agent 逻辑，**零重写**；
- 现有 MVP 可作为「离线演示版」保留（无 key 也能跑），全栈版作为「产品化版」；
- 不删除 MVP，二者并存，面试时可讲「MVP 快速验证 → 全栈分层产品化」的演进判断。
